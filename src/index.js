import './style.css';
import 'boxicons';
import { createNew } from './createNewList';

// Load DOM.

(function DOMHandler() {

    // Attach event listeners to create new lists / tags.
    createNew();

})();